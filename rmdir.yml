---
- hosts: all
  become: true
  tasks: 
    - name: template file onto remote hosts
      template:
        src: /home/ec2-user/templates/my_temp.conf.j2
        dest: /etc/my_temp.conf
        
    

